public enum TipoToken{
    PROGAM, BEGIN, END, READ, WRITE, IF, THEN, ELSE, WHILE, DO, INI,
    ID, NUMBER, ASSIGN, PLUS, MINUS, TIMES, DIVIDE, EQ, LT, GT, LPAREN, RPAREN,
    LBRACE, RBRACE, SEMICOLON, COLON, COMMA, DOT, WHITESPACE, COMMENT, LE, GE,
    NUMBERREAL, VAR
}