public enum TipoToken{ //tokens da linguagem(minilang)
    PROGRAM, BEGIN, READ, WRITE, IF, THEN, ELSE, WHILE, DO, INT,
    ID, NUMBER, ASSIGN, PLUS, MINUS, TIMES, DIVIDE, EQ, LT, GT, LPAREN, RPAREN,
    LBRACE, RBRACE, SEMICOLON, COLON, COMMA, DOT, WHITESPACE, COMMENT, LE, GE,
    NUMBERREAL, VAR, FLOAT, END
}

//enum é um dado especial usado para definir um conjunto fixo de constantes